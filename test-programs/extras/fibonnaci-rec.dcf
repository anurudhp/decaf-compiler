class Program {
	int fib(int n) {
		if (n <= 2) {
			return 1;
		}
		return fib(n - 1) + fib(n - 2);
	}
	void main() {
		int n;
		n = callout("read_int");
		callout("write_int", fib(n));
		callout("write_char", '\n');
	}
}